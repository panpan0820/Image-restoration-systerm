# 1. 镜像源置顶，优先国内下载
-i https://pypi.tuna.tsinghua.edu.cn/simple

# 2. 锁定 Streamlit 稳定版 + 兼容的 rich 版本（解决冲突）
streamlit==1.28.2
rich==13.7.0

# 3. 安装 OpenCV 终极无依赖版（彻底绕过 libGL，服务器专用）
opencv-python-headless==4.8.0.76

# 4. 基础依赖（固定兼容版本）
Pillow==10.1.0
numpy==1.24.3
